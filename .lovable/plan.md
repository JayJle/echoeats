## 改动
把三层打分的权重重新分配，总分仍是 100：
- **Layer 2 基础分（Google 评分）**：40 → **20**
- **Layer 3 匹配分（条件契合度）**：60 → **80**

更看重"是否符合你的需求"，少看重单纯的高评分。

## 具体数字（都在 `src/lib/echo.functions.ts`）

### Layer 2
- 公式从 `adjRating × 8`（封顶 40）改为 `adjRating × 4`（封顶 20）

### Layer 3 子项按 60→80 等比放大（×4/3）
| 项目 | 原 | 新 |
|---|---|---|
| AI matchScore 系数 | × 0.35 | × **0.47** |
| 硬条件 ❌ 扣分 | 权重 × 8 | 权重 × **10.7** |
| 硬条件 ⚠️ 扣分 | 权重 × 2 | 权重 × **2.7** |
| 软偏好 ✅ 加分 | 权重 × 5（封顶 15） | 权重 × **6.7**（封顶 **20**） |
| 软偏好 ❌ 扣分 | 权重 × 3 | 权重 × **4** |
| 避雷词 ❌ 扣分 | 权重 × 10 | 权重 × **13.3** |
| 菜品 ✅ 加分 | +4（封顶 12） | +**5.3**（封顶 **16**） |
| 多路召回 | +3 / +6 / +10 | +**4** / +**8** / +**13** |
| Layer 3 总封顶 | 60 | **80** |

### 阶位阈值不变
- perfect ≥ 80，high ≥ 65，partial 其他 —— 这两个阈值在新分布下依然合理。

### 不动的部分
- Layer 1 准入规则、置信度机制、匹配详情显示逻辑全部不变。
- 一票否决后压到 ≤30 的规则不变。