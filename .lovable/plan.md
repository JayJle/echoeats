## 改动

把"AI 自动推断料理品类"的数量上限从 1–3 个改成 **1–2 个**（用户跳过 cuisines 时）。

## 文件

`src/lib/echo.functions.ts`，共 4 处文案：

- L248 — 主 prompt 数据描述行："推断 1-3 个料理候选" → "推断 1-2 个料理候选"
- L256 — 字段规则："推断 1-3 个最相关的料理类型" → "推断 1-2 个最相关的料理类型"
- L280 — 品类级约束规则："主动产出 1–3 个匹配品类" → "主动产出 1–2 个匹配品类"
- L398 — 重试指令："给出 1-3 个最相关的**具体**料理品类" → "给出 1-2 个最相关的**具体**料理品类"

## 不动

- 用户**已手动选择**的 cuisines 仍按用户原样使用，不截断。
- 召回 / 去重 / AI 排序链路均不变。

## 预期效果

- AI 自动推断时最多 2 个品类 → 候选池更聚焦，token 进一步下降。
